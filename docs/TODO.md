# TODO

- Wire real PostgreSQL migrations into local bootstrap docs
- Add optimistic UI updates for run progression
- Add retry controls in the approval and run detail views
- Extend the engineer step to apply structured code patches beyond `.aiteams` repo artefacts
- Replace inline queue fallback with durable worker commands for non-local deployments

